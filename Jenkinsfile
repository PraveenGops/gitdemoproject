pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                echo 'Hello World'
            }
         stage('deploy') {
            steps {
                echo 'Application deployed'
            }
        }
    }
  }
}
